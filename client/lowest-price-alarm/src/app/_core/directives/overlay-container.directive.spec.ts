import { OverlayContainerDirective } from './overlay-container.directive';

describe('OverlayContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayContainerDirective();
    expect(directive).toBeTruthy();
  });
});
